- [x] Verify that the copilot-instructions.md file in the .github directory is created.
- [x] Clarify Project Requirements
- [x] Scaffold the Project
- [x] Customize the Project
- [x] Install Required Extensions (no required extensions specified)
- [ ] Compile the Project (blocked: node/npm and uvicorn dependency not installed in environment)
- [x] Create and Run Task
- [ ] Launch the Project (pending user confirmation for debug launch)
- [x] Ensure Documentation is Complete

Progress notes:
- Frontend scaffold created in `frontend` with Vite-compatible React starter files.
- Backend scaffold created in `backend` with FastAPI app and `/health` endpoint.
- Root docs and ignore rules added (`README.md`, `.gitignore`).
- VS Code task added in `.vscode/tasks.json` for backend launch.
